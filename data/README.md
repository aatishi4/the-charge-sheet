# Local data

Optional datasets for offline lookups, created by `tools/build-data.mjs` and listed in `manifest.json`.
The site loads whatever the manifest lists when it starts. An empty list is fine.
